// Emit code compatible with the NASM assembler
void EmitNasm(char* filename, struct tree_node* root);

// Emit code compatible with the FASM assembler
void EmitFasm(char* filename, struct tree_node* root, char* org);


